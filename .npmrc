# HeroUI v3 ships as many small @heroui/* packages and relies on framer-motion.
# pnpm's strict node_modules layout hides these from Tailwind v4's scanner and
# from heroui's runtime resolution unless they are hoisted to the root.
public-hoist-pattern[]=*@heroui*
public-hoist-pattern[]=*framer-motion*
